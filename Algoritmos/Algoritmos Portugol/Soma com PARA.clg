algoritmo semNome;
// Síntese
//  Objetivo:  
//  Entrada :
//  Saída   :


principal
	// Declarações

	inteiro numero, contador, soma;
soma = 0;
		para (contador de 1 ate 10 passo 1) faca
		escreva (" Informe um valor: ");
		leia (numero);
			soma = numero + soma;
		fimPara
	escreval (soma);



	// Instruções

fimPrincipal
